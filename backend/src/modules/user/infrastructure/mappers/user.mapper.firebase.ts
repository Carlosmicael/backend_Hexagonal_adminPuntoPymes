/*import { User } from '../../../domain/user.entity';

export class UserMapper {
  static toDomain(doc: FirebaseFirestore.DocumentData, id: string): User {
    return new User(id, doc.name, doc.email);
  }

  static toPersistence(user: User): FirebaseFirestore.DocumentData {
    return {
      name: user.name,
      email: user.email,
    };
  }
}
*/
